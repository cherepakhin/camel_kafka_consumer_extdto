https://kafka-school.ru/blog/kafka-offsets/

Смещение (offset) в Kafka — это индекс (порядковый номер), указывающий на положение записи в разделе (partititon) топика.

Информация о смещениях (порядковые номера всех сообщений) хранится в специальном топике __consumer_offsets.

